x = 10
y = 20

u = c(1, 2, 3, 4, 5) # Combine values (Multiple Values) (Creates a List)
print(u)

# rm(x,y,z) # Removes the selected variables
# print(x)

print(mean(u))
print(median(u))
print(sum(u))

install.packages("caret")
library(caret)
 
str(data)

str(data)